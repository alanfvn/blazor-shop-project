namespace ShopOnline.Web {
    public class HardCoded {

        /*
         * As we didn't implement a user control
         * system we are using hardcoded values
         * for this example.
         */
        public const int UserId = 1;
        public const int CartId = 1;
    }
}
